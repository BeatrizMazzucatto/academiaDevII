# Diagrama de Classes UML - AcademiaDev

Diagrama de Classes UML representando a estrutura do sistema AcademiaDev seguindo a Clean Architecture.

## 📊 Diagrama de Classes UML

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                  DOMAIN LAYER                                        │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐         ┌──────────────────────┐
│   DifficultyLevel    │         │    CourseStatus      │         │   SubscriptionPlan   │
│     (enum)           │         │       (enum)         │         │     (abstract)       │
├──────────────────────┤         ├──────────────────────┤         ├──────────────────────┤
│ BEGINNER             │         │ ACTIVE               │         │ +name: String        │
│ INTERMEDIATE         │         │ INACTIVE             │         │ +maxActiveEnrollments│
│ ADVANCED             │         └──────────────────────┘         │ +canEnroll(int): bool│
└──────────────────────┘                                         └──────────┬───────────┘
                                                                             │
┌──────────────────────┐         ┌──────────────────────┐                  │
│       Course         │         │        User           │                  │
│     (entity)         │         │      (abstract)       │                  │
├──────────────────────┤         ├──────────────────────┤                  │
│ -title: String       │         │ -name: String         │                  │
│ -description: String │         │ -email: String        │                  │
│ -instructorName: Str │         │ +isAdmin(): boolean   │                  │
│ -durationInHours: in │         └──────────┬────────────┘                  │
│ -difficultyLevel:    │                    │                               │
│   DifficultyLevel    │                    │                               │
│ -status: CourseStatus│                    │                               │
│ +isActive(): boolean │                    │                               │
└──────────────────────┘                    │                               │
                                            │                               │
                    ┌───────────────────────┴──────────────────┐           │
                    │                                           │           │
        ┌───────────▼────────────┐                  ┌──────────▼──────────┐
        │       Student          │                  │       Admin         │
        │       (entity)         │                  │      (entity)       │
        ├────────────────────────┤                  ├─────────────────────┤
        │ -subscriptionPlan:     │                  │ +isAdmin(): boolean │
        │  SubscriptionPlan      │                  └─────────────────────┘
        │ +canEnroll(int): bool  │
        └────────────┬───────────┘
                     │
                     │   ┌──────────────────────────────┐
                     │   │   BasicPlan                  │
                     │   │   (entity)                   │
                     └───┤   extends SubscriptionPlan   │
                         │   +maxActiveEnrollments: 3   │
                         └──────────────────────────────┘
                     │
                     │   ┌──────────────────────────────┐
                     │   │   PremiumPlan                │
                     │   │   (entity)                   │
                     └───┤   extends SubscriptionPlan   │
                         │   +maxActiveEnrollments: -1  │
                         └──────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐         ┌──────────────────────┐
│     Enrollment       │         │   SupportTicket      │         │  BusinessException   │
│     (entity)         │         │      (entity)        │         │    (exception)       │
├──────────────────────┤         ├──────────────────────┤         ├──────────────────────┤
│ -student: Student    │         │ -title: String       │         │ +BusinessException() │
│ -course: Course      │         │ -message: String     │         │   extends RuntimeEx  │
│ -progress: double    │         │ -user: User          │         └──────────┬───────────┘
│ -active: boolean     │         └──────────────────────┘                    │
│ +cancel(): void      │                                                 ┌────▼──────────┐
│ +setProgress(double) │                                                 │EnrollmentExcep│
└──────────────────────┘                                                 │   (exception) │
                                                                         │extends BusEx  │
                                                                         └───────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                               APPLICATION LAYER                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────┐         ┌──────────────────────┐         ┌──────────────────────┐
│  CourseRepository    │         │   UserRepository     │         │ EnrollmentRepository │
│    (interface)       │         │    (interface)       │         │    (interface)       │
├──────────────────────┤         ├──────────────────────┤         ├──────────────────────┤
│ +save(Course): void  │         │ +save(User): void    │         │ +save(Enrollment)    │
│ +findByTitle(String):│         │ +findByEmail(String):│         │ +findByStudent(Stud) │
│   Optional<Course>   │         │   Optional<User>     │         │ +findActiveByStudent │
│ +findAll(): List     │         │ +findAll(): List     │         │ +countActiveByStudent│
│ +findByDifficulty... │         │ +findAllStudents()   │         │ +findByStudentAndCrs │
│ +findActiveCourses() │         └──────────────────────┘         └──────────────────────┘
└──────────────────────┘

┌──────────────────────┐
│  SupportTicketQueue  │
│    (interface)       │
├──────────────────────┤
│ +addTicket(Ticket)   │
│ +nextTicket(): Opt   │
│ +isEmpty(): boolean  │
│ +size(): int         │
└──────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              USE CASES (Application)                                  │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│   MatricularAlunoUseCase         │
├──────────────────────────────────┤
│ -enrollmentRepository: ...       │
│ -courseRepository: ...           │
│ +execute(Student, String): Enroll│
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   AtualizarProgressoUseCase      │
├──────────────────────────────────┤
│ -enrollmentRepository: ...       │
│ +execute(Student, String, double)│
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   CancelarMatriculaUseCase       │
├──────────────────────────────────┤
│ -enrollmentRepository: ...       │
│ +execute(Student, String): void  │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   AbrirTicketUseCase             │
├──────────────────────────────────┤
│ -ticketQueue: ...                │
│ +execute(String, String, User)   │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   AtenderTicketUseCase           │
├──────────────────────────────────┤
│ -ticketQueue: ...                │
│ +execute(): SupportTicket        │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   GerarRelatorioCursosPorNivel   │
├──────────────────────────────────┤
│ -courseRepository: ...           │
│ +execute(DifficultyLevel): List  │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   GerarRelatorioInstrutoresUnicos│
├──────────────────────────────────┤
│ -courseRepository: ...           │
│ +execute(): Set<String>          │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   GerarRelatorioAlunosPorPlano   │
├──────────────────────────────────┤
│ -userRepository: ...             │
│ +execute(): Map<Plan, List<Stud>>│
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   CalcularMediaProgressoUseCase  │
├──────────────────────────────────┤
│ -enrollmentRepository: ...       │
│ +execute(): double               │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   IdentificarAlunoMaisMatriculado│
├──────────────────────────────────┤
│ -enrollmentRepository: ...       │
│ -userRepository: ...             │
│ +execute(): Optional<Student>    │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   AtualizarStatusCursoUseCase    │
├──────────────────────────────────┤
│ -courseRepository: ...           │
│ +execute(String, CourseStatus)   │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│   AlterarPlanoAlunoUseCase       │
├──────────────────────────────────┤
│ -userRepository: ...             │
│ -enrollmentRepository: ...       │
│ +execute(String, Plan): Student  │
└──────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              INFRASTRUCTURE LAYER                                    │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│ CourseRepositoryEmMemoria        │
│   implements CourseRepository    │
├──────────────────────────────────┤
│ -courses: Map<String, Course>    │
│ +save(Course): void              │
│ +findByTitle(String): Optional   │
│ +findAll(): List<Course>         │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ UserRepositoryEmMemoria          │
│   implements UserRepository      │
├──────────────────────────────────┤
│ -users: Map<String, User>        │
│ +save(User): void                │
│ +findByEmail(String): Optional   │
│ +findAll(): List<User>           │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ EnrollmentRepositoryEmMemoria    │
│   implements EnrollmentRepository│
├──────────────────────────────────┤
│ -enrollments: List<Enrollment>   │
│ +save(Enrollment): void          │
│ +findByStudent(Student): List    │
│ +countActiveByStudent(Student)   │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│ SupportTicketQueueEmMemoria      │
│   implements SupportTicketQueue  │
├──────────────────────────────────┤
│ -tickets: Queue<SupportTicket>   │
│   (ArrayDeque)                   │
│ +addTicket(Ticket): void         │
│ +nextTicket(): Optional          │
│ +isEmpty(): boolean              │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│      ConsoleController           │
├──────────────────────────────────┤
│ -view: ConsoleView               │
│ -userRepository: ...             │
│ -courseRepository: ...           │
│ -enrollmentRepository: ...       │
│ -matricularAlunoUseCase: ...     │
│ -atualizarProgressoUseCase: ...  │
│ ... (outros use cases)           │
│ +start(): void                   │
│ +authenticate(): void            │
│ +mainLoop(): void                │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│        ConsoleView               │
├──────────────────────────────────┤
│ +showWelcome(): void             │
│ +showMainMenu(boolean): void     │
│ +showCourses(List): void         │
│ +showEnrollments(List): void     │
│ +showCsvExport(String): void     │
│ ... (outros métodos de exibição) │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│     GenericCsvExporter           │
├──────────────────────────────────┤
│ +exportToCsv(List, List): String │
│ -validateFields(Class, List)     │
│ -getAvailableFields(Class)       │
│ -getFieldValue(Object, String)   │
│ (Usa Reflection)                 │
└──────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                                  MAIN LAYER                                          │
└─────────────────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────┐
│            Main                  │
├──────────────────────────────────┤
│ +main(String[]): void            │
│ - Cria repositórios (infrastructure)│
│ - Cria use cases (application)   │
│ - Cria UI (infrastructure)       │
│ - Popula dados iniciais          │
│ - Inicia aplicação               │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│        InitialData               │
├──────────────────────────────────┤
│ +populate(UserRepo, CourseRepo)  │
│ -populateUsers(UserRepo): void   │
│ -populateCourses(CourseRepo)     │
└──────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────┐
│                            RELACIONAMENTOS (Setas)                                    │
└─────────────────────────────────────────────────────────────────────────────────────┘

Dependências (direção das setas apontam para dentro):

Infrastructure → Application → Domain

Exemplos:
- ConsoleController usa MatricularAlunoUseCase
- MatricularAlunoUseCase usa CourseRepository (interface)
- CourseRepositoryEmMemoria implementa CourseRepository
- Todos usam entidades do Domain

Isolamento:
- Domain não conhece ninguém
- Application conhece apenas Domain
- Infrastructure conhece Application e Domain
- Main conhece todos (composição)
```

## 📋 Descrição das Classes

### Domain Layer

**Entidades:**
- `Course`: Representa um curso da plataforma
- `User`: Classe abstrata para usuários
- `Admin`: Extende User, representa administradores
- `Student`: Extende User, representa alunos com plano de assinatura
- `SubscriptionPlan`: Classe abstrata para planos de assinatura
- `BasicPlan`: Extende SubscriptionPlan, permite 3 matrículas ativas
- `PremiumPlan`: Extende SubscriptionPlan, permite matrículas ilimitadas
- `Enrollment`: Representa uma matrícula de aluno em curso
- `SupportTicket`: Representa um ticket de suporte

**Enums:**
- `DifficultyLevel`: Níveis de dificuldade dos cursos
- `CourseStatus`: Status do curso (ACTIVE/INACTIVE)

**Exceções:**
- `BusinessException`: Exceção genérica de negócio
- `EnrollmentException`: Exceção específica para matrículas

### Application Layer

**Interfaces de Repositório:**
- `CourseRepository`: Contrato para persistência de cursos
- `UserRepository`: Contrato para persistência de usuários
- `EnrollmentRepository`: Contrato para persistência de matrículas
- `SupportTicketQueue`: Contrato para fila de tickets (FIFO)

**Use Cases (13 implementados):**
- `MatricularAlunoUseCase`: Matricula aluno em curso
- `AtualizarProgressoUseCase`: Atualiza progresso do aluno
- `CancelarMatriculaUseCase`: Cancela matrícula
- `AbrirTicketUseCase`: Abre ticket de suporte
- `AtenderTicketUseCase`: Processa ticket da fila
- `GerarRelatorioCursosPorNivelUseCase`: Relatório por nível
- `GerarRelatorioInstrutoresUnicosUseCase`: Instrutores únicos
- `GerarRelatorioAlunosPorPlanoUseCase`: Alunos agrupados
- `CalcularMediaProgressoUseCase`: Média de progresso
- `IdentificarAlunoMaisMatriculadoUseCase`: Aluno mais ativo
- `AtualizarStatusCursoUseCase`: Atualiza status do curso
- `AlterarPlanoAlunoUseCase`: Altera plano do aluno

### Infrastructure Layer

**Persistência:**
- `CourseRepositoryEmMemoria`: Implementa CourseRepository usando Map
- `UserRepositoryEmMemoria`: Implementa UserRepository usando Map
- `EnrollmentRepositoryEmMemoria`: Implementa EnrollmentRepository usando List
- `SupportTicketQueueEmMemoria`: Implementa SupportTicketQueue usando ArrayDeque

**UI:**
- `ConsoleController`: Coordena a interação do usuário
- `ConsoleView`: Responsável apenas por exibição

**Utils:**
- `GenericCsvExporter`: Exportação CSV usando Reflection

### Main Layer

- `Main`: Composição e injeção manual de dependências
- `InitialData`: Popula dados iniciais

## 🔗 Relacionamentos

**Herança:**
- `Admin extends User`
- `Student extends User`
- `BasicPlan extends SubscriptionPlan`
- `PremiumPlan extends SubscriptionPlan`
- `EnrollmentException extends BusinessException`

**Implementação:**
- `CourseRepositoryEmMemoria implements CourseRepository`
- `UserRepositoryEmMemoria implements UserRepository`
- `EnrollmentRepositoryEmMemoria implements EnrollmentRepository`
- `SupportTicketQueueEmMemoria implements SupportTicketQueue`

**Composição/Agregação:**
- `Student` possui `SubscriptionPlan`
- `Enrollment` possui `Student` e `Course`
- `SupportTicket` possui `User`
- UseCases possuem Repositories (via construtor)
- `ConsoleController` possui UseCases e Repositories (via construtor)

**Dependência (via construtor):**
- UseCases recebem interfaces de repositório
- `ConsoleController` recebe UseCases e Repositories
- `Main` cria todas as dependências

## ✅ Regra da Dependência Visualizada

```
┌─────────────────────┐
│  Infrastructure     │
│  (depende de)       │────┐
└─────────────────────┘    │
                           │
┌─────────────────────┐    │
│   Application       │    │
│  (depende de)       │────┼───┐
└─────────────────────┘    │   │
                           │   │
┌─────────────────────┐    │   │
│     Domain          │    │   │
│  (não depende)      │◄───┴───┘
└─────────────────────┘

Setas apontam sempre para dentro (Domain) ✅
```

---

**Nota:** Este diagrama foi gerado em formato texto (Markdown) para facilitar a visualização e versionamento. Para gerar um diagrama visual (PNG/SVG), recomenda-se usar ferramentas como:
- PlantUML
- draw.io
- Lucidchart
- Astah
- Enterprise Architect

